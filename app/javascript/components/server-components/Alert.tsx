// Backward compatibility until we remove server components
import Alert, { showAlert, type AlertPayload } from "$app/components/Alert";

export { showAlert, type AlertPayload };
export default Alert;
